<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://bootswatch.com/5/cosmo/bootstrap.min.css"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
      crossorigin="anonymous"
    ></script>
    <title><%= title %></title>
  </head>
  <body>
    <nav
      class="navbar bg-dark border-bottom border-body d-flex justify-space"
      data-bs-theme="dark"
    >
      <h1 class="text-white mx-3">Tattler</h1>
      <a href="/logout" class="btn btn-danger mx-3">LogOut</a>
    </nav>
    <h2 class="text-center mt-3">Menu</h2>

    <main class="container-fluid">
      <div class="d-flex justify-content-center align-items-center">
        <form id="form" method="post" class="d-flex flex-column gap-3">
          <label for="name" class="d-flex flex-column"
            ><i class="text-danger position-absolute translate-middle fs-4">*</i
            >Name:

            <input id="name" type="text" name="name" required />
          </label>
          <label for="location" class="d-flex flex-column">
            Location:
            <input
              id="location"
              type="text"
              name="location"
              placeholder="Ej: State, city, street"
            />
            <small>Separate state, city, and street with a comma.</small>
          </label>
          <label for="category" class="d-flex flex-column">
            Category:
            <input
              id="category"
              type="text"
              name="category"
              placeholder="Ej:Sea food, italian, mexican"
            />
            <small>Separate each category with a comma.</small>
          </label>
          <div
            class="container d-flex justify-content-between align-items-center gap-5"
          >
            <label for="open" class="d-flex flex-column">
              Open:
              <input id="open" type="time" name="open" />
            </label>
            <label for="close" class="d-flex flex-column">
              Close:
              <input id="close" type="time" name="close" />
            </label>
          </div>
          <label for="stars" class="d-flex flex-column">
            Stars:
            <input
              id="stars"
              type="number"
              name="stars"
              max="5"
              min="0"
              placeholder="0 - 5"
            />
          </label>
          <label for="comments" class="d-flex flex-column">
            Comments:
            <textarea
              id="comments"
              type="text"
              name="comments"
              cols="25"
              rows="3"
            ></textarea>
          </label>
          <div class="m-auto" id="btns-container">
            <input type="submit" value="Resgister" class="btn btn-primary" />
            <input type="submit" value="Delete" class="btn btn-danger" />
            <input type="submit" value="Edit" class="btn btn-warning" />
          </div>
          <small
            >Documentation available at:
            <a href="/api/docs/v1">
              https://tattler-api.onrender.com/api/docs/v1</a
            ></small
          >
        </form>
      </div>
    </main>

    <script>
      const form = document.getElementById('form')

      document
        .getElementById('btns-container')
        .addEventListener('click', (e) => {
          if (e.target.value === 'Resgister')
            form.action = '/api/locals/new-local'
          if (e.target.value === 'Delete')
            form.action = '/api/locals/delete-local'
          if (e.target.value === 'Edit') form.action = '/api/locals/edit-local'
          form.submit()
        })
    </script>
  </body>
</html>
